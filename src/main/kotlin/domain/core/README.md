### :core 모듈 규칙

1. 현재 위치(:core)에 있는 파일들은 외부 종속성이 없고 순수 Kotlin/Java 객체(VO)만 있어야 한다.
2. 다른 도메인 모델(엔티티)도 해당 VO 객체를 참조해서 사용가능하다. (1번이 만족한다는 가정하에)
3. :domain 내에 있는 파일만 :core 모듈에 접근 가능하다. (즉, :core 모듈은 :domain 모듈 한정 가장 상위계층)